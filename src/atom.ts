import { atom } from "recoil";

export const AmenuToggle = atom({
  key: "Banner/MenuToggle",
  default: false,
});

export const AsearchToggle = atom({
  key: "Banner/SearchToggle",
  default: false,
});
